// Expo's entry point. It registers App as the root component.
// You never need to change this file.
import { registerRootComponent } from 'expo';

import App from './App';

registerRootComponent(App);
